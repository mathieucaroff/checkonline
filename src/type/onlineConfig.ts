export interface OnlineConfig {
   clear: boolean
   debug: boolean
   period: string
   fast: boolean
   width: number
   height: number
   drawSpeed: number
   targetList: string
   targetCount: number
}
